<%= form_for(@tipo_endereco) do |f| %>
  <% if @tipo_endereco.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_endereco.errors.count, "error") %> prohibited this tipo_endereco from being saved:</h2>

      <ul>
      <% @tipo_endereco.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
